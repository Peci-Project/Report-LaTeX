\documentclass[12pt, a4paper]{report}

% ─── Packages ────────────────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{fancyhdr}
\usepackage{indentfirst}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{url}
\usepackage{cite}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{float}
\usepackage{lipsum} % Remove when filling in real content

% ─── Page geometry ───────────────────────────────────────────────────────────
\geometry{
    a4paper,
    top=2.5cm,
    bottom=2.5cm,
    left=3cm,
    right=2.5cm
}

% ─── Line spacing ────────────────────────────────────────────────────────────
\onehalfspacing

% ─── Header / footer ─────────────────────────────────────────────────────────
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\leftmark}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ─── Hyperlink colours ───────────────────────────────────────────────────────
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=blue
}

% ─── Code listing style ──────────────────────────────────────────────────────
\lstset{
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{gray},
    stringstyle=\color{red},
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny\color{gray}
}

% ─────────────────────────────────────────────────────────────────────────────
%  DOCUMENT
% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

% ─── Title page ──────────────────────────────────────────────────────────────
\begin{titlepage}
    \centering
    \vspace*{1cm}

    {\large \textbf{Universidade de Aveiro}}\\[0.3cm]
    {\large Departamento de Eletrónica, Telecomunicações e Informática}\\[2cm]

    % \includegraphics[width=0.3\textwidth]{logo}\\[2cm] % uncomment and replace 'logo' with your logo file

    {\LARGE \textbf{Project Title}}\\[0.5cm]
    {\large Technical Report}\\[2cm]

    {\large \textbf{Equipa}}\\[0.2cm]
    {\large André Reis: 119814}\\[0.2cm]
    {\large Vasco Oliveira: 119113}\\[0.2cm]
    {\large Pedro Ferreira: 119240}\\[0.2cm]
    {\large Carlos Ramos: 119873}\\[0.2cm]
    {\large Dinis Malhado: 120420}\\[0.2cm]
    {\large Simão Pereira: 120459}\\[2cm]

    {\large Orientador: Diogo Gomes}\\[2cm]


    {\large \today}

    \vfill
\end{titlepage}

% ─── Abstract ────────────────────────────────────────────────────────────────
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}
% TODO: Write the abstract (150–250 words recommended).
% Summarise: motivation, problem, approach, main results and conclusions.
For over a decade, the api.web.ua.pt platform served as a vital digital backbone for the University of Aveiro,
centralizing essential services ranging from canteen menus to academic ticketing. However, as the digital landscape
evolved, the original infrastructure began to show its age, struggling with outdated security protocols, poor readability,
and a lack of modern architectural standards. These mounting technical limitations eventually led to the decommissioning
of the legacy system, sparking the urgent need for a complete, from-the-ground-up rebuild to better serve the modern student body.
This project outlines the comprehensive modernization of the university’s API ecosystem. The reconstruction began by revitalizing
core endpoints—including canteen services (sas), RSS-to-JSON translation tools (Rss2Json), institutional data (ArcGIS),
real-time parking availability (parques), and academic services (sac). By employing current programming languages and libraries,
we addressed the fundamental bugs and inefficiencies of the previous system. To safeguard these assets against external threats,
the new APIs were integrated into a robust WSO2 management layer, unified under the university’s proprietary identity
provider (IdpUa). The result is a secure, high-performance platform complemented by a dedicated documentation portal
and an iterative testing framework, ensuring that the university's digital tools are as reliable and accessible as the
services they represent.

\vspace{1em}

% ─── Table of contents ───────────────────────────────────────────────────────
\tableofcontents
\listoffigures     % remove if you have no figures
\listoftables      % remove if you have no tables

% ─────────────────────────────────────────────────────────────────────────────
%  CHAPTER 1 – Introduction
% ─────────────────────────────────────────────────────────────────────────────
\chapter{Introduction}
\label{chap:introduction}

\section{Context and Motivation}
% TODO: Describe the context of the project and why it is relevant.
Anteriormente, a universidade disponibilizava a plataforma API (Academic Playground \& Innovation), 
acessível no endereço api.web.ua.pt, que fornecia dados vitais para a comunidade, como a lotação de parques
de estacionamento, senhas de serviços académicos, ementas das cantinas e informações
sobre as unidades da universidade. 

No entanto, este serviço teve de ser descontinuado devido a falhas críticas.
Para resolver este problema, foi proposta a criação de uma nova
API Gateway institucional, perfeitamente controlada, auditável e escalável.


\section{Problem Statement}
% TODO: Clearly define the problem being addressed.
A descontinuação do serviço deve-se principalmente a vulnerabilidades de segurança e
à falta de otimização do sistema. O sistema baseava-se numa versão de PHP muito antiga que já tinha atingido o
seu "End of Life", o que significa que deixou de receber atualizações
e ficou vulnerável a problemas de segurança.




Isto fez com que a integração entre sistemas
fosse difícil e que a experiência da comunidade académica fosse prejudicada.



\section{Objetivos}
% TODO: List the main goals of the project.
Os objetivos principais deste projeto são:
\begin{itemize}
    \item Transcrever o código obsoleto e passar toda a infraestrutura para a linguagem Python,
    utilizando a tecnologia API Gateway WSO2 para ter a API funcional e segura;
    \item Garantir que todas as APIs já existentes correm de forma estável e funcional na nova infraestrutura,
    de maneira a não prejudicar a experiência da comunidade académica;
    \item Desenvolver uma arquitetura escalável e fácil de manter, para que seja possível adicionar
    novas APIs que sejam relevantes no futuro.
\end{itemize}

\section{Document Structure}
% TODO: Briefly describe what each chapter covers.
This report is organised as follows.
Chapter~\ref{chap:related_work} presents related work, tools and technologies.
Chapter~\ref{chap:requirements} describes the requirements elicitation and system architecture.
Chapter~\ref{chap:expected_results} outlines the expected results.

% ─────────────────────────────────────────────────────────────────────────────
%  CHAPTER 2 – Related Work, Tools and Technologies
% ─────────────────────────────────────────────────────────────────────────────
\chapter{Related Work, Tools and Technologies}
\label{chap:related_work}

\section{Related Work}

Two previous projects developed at the University of Aveiro are particularly
relevant to this work: the \emph{OPEN DATA UA} master thesis
and the final report of the \emph{Academic Playground \& Innovation (API)}
project. Together, they provide both the conceptual and
practical foundations on which our project is built.

The \emph{OPEN DATA UA} thesis surveys existing institutional information
sources and web services at UA and proposes a service-oriented architecture
based on REST and SOAP for exposing institutional data (e.g., cantina menus,
academic information) in a standardized way. It also implements a web
portal (APIn) that aggregates services from UA, PT Inovação and SAPO, and
introduces an OAuth-based authorization server to protect access to private or
sensitive web services. This work is mainly focused on identifying data
sources, creating individual web services, and demonstrating how open data and
secure access control can be applied within the university context.

The \emph{Academic Playground \& Innovation} final report documents the
concrete implementation of that vision, namely the api.web.ua.pt portal and
the supporting services running on services.web.ua.pt and identity.ua.pt.
It describes in detail the API portal, the catalogue of services (UA, PT SAPO,
PT Inovação), the documentation and wiki mechanisms, the statistics and
administration interfaces, as well as the deployment of an OAuth 1.0a server
integrated with the UA Identity Provider using SimpleSAMLphp.This
report provides an operational view of how service publication, authentication,
authorization and consumption were handled in practice in the first generation
of the institutional API platform.

Our project can be seen as a next step in this evolution. While \emph{OPEN DATA
UA} and the API portal focused on exposing individual services and providing a
web-based aggregation and documentation layer, they are tightly coupled to
specific technologies and infrastructures that have since become obsolete. In contrast, our work aims at
designing a modern institutional API Gateway and governance model, aligned with
current API management practices. Building on the lessons learned from these previous
projects, we seek to provide a more sustainable, secure and governable
integration layer for present and future UA systems.


\subsection{Similar System / Approach A}
% TODO: Describe a related system or approach.
\lipsum[7]

\subsection{Similar System / Approach B}
% TODO: Describe another related system or approach.
\lipsum[8]

\subsection{Summary and Comparison}
% TODO: Summarise and compare the reviewed work.
\lipsum[9]

\section{Tools and Technologies}
% TODO: Describe the main tools, frameworks and technologies you plan to use.

\subsection{Technology / Framework A}
% TODO: Describe this technology and justify your choice.
\lipsum[10]

\subsection{Technology / Framework B}
% TODO: Describe this technology and justify your choice.
\lipsum[11]

\subsection{Technology / Framework C}
% TODO: Describe this technology and justify your choice.
\lipsum[12]

% ─────────────────────────────────────────────────────────────────────────────
%  CHAPTER 3 – Requirements Elicitation and Architecture
% ─────────────────────────────────────────────────────────────────────────────
\chapter{Requirements Elicitation and Architecture}
\label{chap:requirements}

\section{Requirements Elicitation}
% TODO: Explain how requirements were gathered (interviews, surveys, etc.).
Tou  a fazer isto, não mexam nisto pls :)  by pedro

\subsection{Functional Requirements}
% TODO: List and describe the functional requirements.
\begin{itemize}
    \item \textbf{FR1 – Centralização e Exposição de Serviços:} Deve existir uma camada WSO2 que atue como API Gateway centralizada para expor os serviços da Universidade de Aveiro de forma uniforme.
    \item \textbf{FR2 – Identidade e Permissões:} O sistema tem de implementar mecanismos de autenticação e autorização, garantindo controlo de acesso por API, de acordo com o perfil de cada utilizador.
    \item \textbf{FR3 – Controlo e Monitorização:} A plataforma deve permitir o controlo e a monitorização detalhada de todos os acessos aos serviços, incluindo registo e rastreabilidade das chamadas efetuadas.
    \item \textbf{FR4 – Disponibilização de Documentação:} Deve estar disponível a documentação necessária para a utilização de cada API, incluindo descrição dos endpoints, parâmetros, autenticação e exemplos de utilização.
    \item \textbf{FR5 – Continuidade e Compatibilidade:} A plataforma deve manter os mesmos endpoints, assegurando compatibilidade com os sistemas já integrados e evitando interrupções de serviço.
\end{itemize}

\subsection{Non-Functional Requirements}
% TODO: List and describe the non-functional requirements (performance, security, etc.).
\begin{itemize}
    \item \textbf{NFR1 – Segurança:} O sistema deve cumcomercialprir boas práticas de segurança, mitigando as vulnerabilidades identificadas na solução anterior e protegendo dados e acessos.
    \item \textbf{NFR2 – Desempenho:} Os pedidos à API devem ser processados, em condições normais de operação, em menos de 3 segundos.
    \item \textbf{NFR3 – Escalabilidade Técnica:} A API Gateway deve suportar aumento de carga e crescimento do número de consumidores sem degradação significativa do serviço.
    \item \textbf{NFR4 – Escalabilidade Funcional:} A introdução e integração de novas APIs deve ser simples, rápida e com impacto mínimo nos serviços existentes.
    \item \textbf{NFR5 – Manutenibilidade:} O sistema deve adotar boas práticas de desenvolvimento e manter o código legível, modular e bem documentado, de forma a facilitar a manutenção corretiva, evolutiva e preventiva.
    \item \textbf{NFR6 – Usabilidade da Interface:} O portal de documentação deve apresentar uma interface simples, clara e intuitiva, promovendo a adoção e utilização dos serviços disponibilizados.
\end{itemize}

\section{System Architecture}
The platform is organised into four distinct layers, as illustrated in
Figure~\ref{fig:architecture}: the \textit{User Interface}, the \textit{WSO2 API Gateway},
the \textit{FastAPI backend}, and the \textit{Backend data sources}.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/architecture_diagram.png}
    \caption{System Architecture Overview}
    \label{fig:architecture}
\end{figure}
 
\subsection{User Interface --- Academic Playground \& Innovation}
The user-facing layer is the \textit{Academic Playground \& Innovation} portal. It serves
two purposes: providing documentation on how to use each API, and allowing interactive
testing of all available endpoints directly in the browser. The goal here is to make it 
easy for people to learn how to integrate the APIs available in their own projetcs, as after 
they are familiar with the methods there is no real use for this layer.
\medskip

The portal was developed using \textbf{Docusaurus}, a static site generator built on
React and maintained by Meta. This choice is justified by its native support for Markdown
and MDX, built-in documentation versioning, and ease of maintenance --- updating content
requires only editing text files, with no need to touch application code, this also facilitates
escalating the number of APIs, as it's easy to make a complete documentation about all it's methods.
The \textit{Academic Playground \& Innovation} portal provides interactive testing of all
available endpoints directly in the browser, following the same approach used by platforms
such as RapidAPI (wich heavily inspired this front-end). This is achieved by embedding \textbf{Stoplight Elements} into the
Docusaurus site, which renders a full API explorer from the OpenAPI specification generated
automatically by FastAPI.

Rather than communicating with the FastAPI backend directly, all test requests issued from
the portal are routed through the \textbf{WSO2 API Gateway}, the same path taken by any
external consumer. This is accomplished by setting the WSO2 public endpoint as the target
server in the OpenAPI specification:

\begin{lstlisting}[caption={OpenAPI server configuration targeting WSO2}]
{
  "servers": [
    { "url": "https://api.ua.pt/v1" }
  ]
}
\end{lstlisting}

With this configuration, the portal's test interface reflects the real behaviour of the
platform in production --- including authentication, rate limiting, and request routing ---
rather than bypassing the gateway layer. For public APIs, such as the canteen menus, no
credentials are required and requests can be issued immediately. For private APIs, Stoplight
Elements supports OAuth2 flows natively, allowing users to authenticate with their
institutional credentials via \texttt{idp.ua.pt} and have the resulting token attached
automatically to all subsequent test requests.

\subsection{WSO2 API Gateway Layer}
All requests from the portal pass through a WSO2 API Manager instance, which acts as the
central API Gateway for the platform. 
For the test requests explained in the previous layer to function correctly from a browser context, 
the WSO2 instance must be configured to include the appropriate \texttt{Access-Control-Allow-Origin} headers 
in its responses, permitting cross-origin requests originating from the portal's domain.
\medskip
This layer is responsible for:
 
\begin{itemize}
    \item \textbf{Authentication and authorisation} --- identity verification for private APIs
          is delegated to \texttt{idp.ua.pt} via OAuth2, this ensures institutional credentials
          are being requested correctly from UA's official identity provider;
    \item \textbf{Request routing} --- the gateway forwards validated requests to the appropriate
          ServicesUAPT service;
    \item \textbf{Monitoring and rate limiting} --- all inbound traffic is logged and can be
          controlled centrally without changes to the backend.
\end{itemize}
 
Public APIs (such as the canteen menus) are accessible without authentication, while APIs
that expose sensitive data require a valid OAuth2 token issued by the university's identity
provider.
 
% TODO: add details about how the WSO2 instance is deployed on the Kubernetes cluster.
 
\subsection{FastAPI Backend - ServicesUAPT}
The core logic of each service is implemented in Python using the FastAPI framework. This layer is completely
replacing the old ServicesUAPT wich was very outdated. 
Each service is isolated in its own router module, making it straightforward to add, remove, or
update individual APIs without affecting the rest of the platform. The services currently
implemented or planned are:
 
\begin{itemize}
    \item \textbf{SAC} --- queue ticket numbers for the Academic Services office;
    \item \textbf{ArcGIS} --- data about university buildings and campus units;
    \item \textbf{Estacionamento UA} --- real-time parking availability;
    \item \textbf{SAS} --- canteen menus, consumed from the UA WSO2 API and cached via Memcached;
    \item \textbf{Rss2Json} --- generic RSS feed to JSON converter;
    \item (Additional services to be integrated in future iterations).
\end{itemize}
 
FastAPI was chosen because it generates OpenAPI-compliant documentation automatically,
which integrates directly with the WSO2 gateway and removes the need to maintain separate
API specifications.
 
\subsection{Data Sources (Backend)}
Depending on the service, the FastAPI layer consumes data from one of this sources:

\begin{itemize}
    \item \textbf{CSV File} --- used by the SAC service for persistent storage of queue
          ticket data, written and read directly from a local CSV file;
    \item \textbf{SOAP Client} --- used by the SAC service to interface with the legacy
          university systems that expose SOAP/XML web services;
    \item \textbf{WSO2 UA} --- the university's own API Gateway, consumed by services
          such as SAS to retrieve data like canteen menus and parking availability.
\end{itemize}
 
\subsection{Deployment}
% TODO: describe the Kubernetes cluster and container orchestration.
The platform is containerised using Docker and deployed on the University of Aveiro's
Kubernetes cluster, ensuring availability, scalability, and ease of rolling updates.

% ─────────────────────────────────────────────────────────────────────────────
%  CHAPTER 4 – Expected Results
% ─────────────────────────────────────────────────────────────────────────────
\chapter{Expected Results}
\label{chap:expected_results}

% TODO: Describe what you expect to achieve at the end of the project.
\lipsum[18]

\section{Deliverables}
% TODO: List the concrete deliverables (prototype, documentation, etc.).
\begin{itemize}
    \item Deliverable 1 – Description;
    \item Deliverable 2 – Description;
    \item Deliverable 3 – Description.
\end{itemize}

\section{Evaluation Criteria}
% TODO: Define how you will evaluate the success of the project.
\lipsum[19]

\section{Project Schedule}
% TODO: Present a timeline (e.g. a Gantt chart or a simple table).
\begin{table}[H]
    \centering
    \caption{Planned project schedule}
    \label{tab:schedule}
    \begin{tabular}{clcc}
        \toprule
        \textbf{Phase} & \textbf{Task} & \textbf{Start} & \textbf{End} \\
        \midrule
        1 & Requirements elicitation      & Mar 2026 & Mar 2026 \\
        2 & Architecture design           & Mar 2026 & Apr 2026 \\
        3 & Implementation                & Apr 2026 & Jun 2026 \\
        4 & Testing and validation        & Jun 2026 & Jul 2026 \\
        5 & Report writing and submission & Jul 2026 & Jul 2026 \\
        \bottomrule
    \end{tabular}
\end{table}

% ─────────────────────────────────────────────────────────────────────────────
%  Bibliography
% ─────────────────────────────────────────────────────────────────────────────
\bibliographystyle{plain}
\bibliography{references} % create a references.bib file with your citations

% ─────────────────────────────────────────────────────────────────────────────
%  Appendices (optional for first part)
% ─────────────────────────────────────────────────────────────────────────────
% \appendix
% \chapter{Appendix A – Additional Material}

\end{document}
